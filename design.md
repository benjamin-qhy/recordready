# RecordReady 前端设计标准

状态：设计草案 v0.1，2026-09-24。供 AI 设计与实现使用；橙色主题、shadcn/ui 已按用户要求纳入方案，视觉方向待用户从三张设计稿中选择。本文不授权正式开发。

## 先读与优先级

开始前阅读本文、`CONTEXT.md`、`docs/口播录制工具-MVP方案.md` 与用户选定设计稿。用户最新明确要求优先；业务行为以 MVP 方案为准，视觉细节以选定设计稿与本文为准。冲突应说明，不擅自改需求。选稿不等于批准正式开发。

当前有效参考：`prototype/screenshots/11-phase1-settings.png` 展示第一期浮窗结构；旧截图中的紫色、旧工作台和视频库不延续。三张新稿是互斥方向，不混拼。选稿前不将任何方案称为最终设计。

## 技术框架与职责

- React + TypeScript + Vite 为前端基础；Tauri 2 为桌面容器，Rust / 平台模块负责真实采集与保存。
- UI 使用 **shadcn/ui**，以 **Radix primitives** 为统一组件基础；样式使用 **Tailwind CSS**，图标统一 **Lucide React**。依赖版本在正式初始化时核对并锁定，当前不安装、不初始化。
- shadcn/ui 组件源码由项目维护；不能将其误当成录屏或原生窗口框架。录制线框、跨窗口穿透、相机预览、浮窗排除须通过原生接口实现，CSS 的 pointer-events 不能证明系统级穿透。
- 基础组件放 `src/components/ui`，产品组合组件与基础组件分层。使用语义变量和已有 variants，不在业务页面零散覆盖按钮颜色、字体或圆角，不混用第二套完整组件库。
- 实现前查官方组件文档和已有组件；组件尚未安装时不写假 import。Dialog/Popover 的网页层级与原生窗口层级分别验证。

## 产品形态与界面层级

桌面应用设计画布基准为 1440×1024。直接进入准备录制状态，不创建侧边栏、工作台、最近录制、营销首页或第二期功能。桌面背景仅表示用户待录内容，不是应用自带内容页。

1. 录制线框：细橙色边界、四角缩放点、顶部拖动把手；上方为尺寸入口。默认 9:16，输出 1080×1920。框内内容不填满主题色。
2. 提词区：独立可移动，允许移出线框；正文高对比、可透过鼠标，底部为稿件/字号/速度/暂停控件。不能把整个窗做成无差别穿透。
3. 摄像头预览：默认框内右下，可调四角和拖动，设置入口清晰；只影响预览，不改变独立摄像头原片。
4. 底部控制条：状态、计时、摄像头、麦克风及音量、暂停提词、开始/停止、保存位置。主操作只有一个；录制时开始变为停止，不新增录像暂停。
5. 设置使用触发点附近 Popover，默认一次展开一个。编辑稿件不跳转独立页面。结果在当前界面显示，部分保存与失败不能只显示会消失的 toast。

## 颜色草案

橙色用于主操作、选中项、聚焦和录制区域边界；中性色承担绝大多数表面。禁止大面积橙色背景、橙紫渐变和无意义发光。

| 语义 token | 浅色方向 | 深色方向 | 用途 |
|---|---|---|---|
| background / foreground | #FAFAF9 / #1C1917 | #1C1917 / #FAFAF9 | 基础表面与正文 |
| card、popover / foreground | #FFFFFF / #1C1917 | #292524 / #FAFAF9 | 独立浮层 |
| primary / primary-foreground | #C2410C / #FFFFFF | #FB923C / #1C1917 | 主按钮与选中态 |
| primary-hover | #9A3412 | #FDBA74 | 主按钮悬停 |
| secondary / secondary-foreground | #F5F5F4 / #292524 | #44403C / #FAFAF9 | 次要操作 |
| muted / muted-foreground | #F5F5F4 / #57534E | #292524 / #D6D3D1 | 辅助文字 |
| accent / accent-foreground | #FFF7ED / #9A3412 | #431407 / #FED7AA | 轻量选中背景 |
| border / input | #D6D3D1 / #78716C | #57534E / #A8A29E | 分隔与输入边界 |
| ring | #C2410C | #FB923C | 键盘聚焦 |
| destructive / foreground | #B91C1C / #FFFFFF | #FCA5A5 / #450A0A | 错误与危险操作 |
| success / foreground | #166534 / #FFFFFF | #86EFAC / #052E16 | 保存成功 |
| capture-outline | #EA580C | #FB923C | 线框，配黑白双层描边适应桌面 |

以上为可实现的候选 tokens，选稿后再冻结，不代表明暗主题切换已进入首版范围。Tailwind 通过 CSS 变量映射到语义类名；业务组件不用随意的色值类。`success`、`capture-outline`、`primary-hover` 是项目扩展，须显式定义。

录制状态用圆点/停止图标、计时与文字共同表达；失败使用错误图标和文案，不单靠红橙色区分。半透明浮窗须验证最差背景对比；提词正文默认接近不透明深底白字，不依赖模糊背景保证可读性。

## 排版、尺寸与密度

字体使用系统无衬线栈（macOS 系统字体、PingFang SC、Windows Segoe UI / Microsoft YaHei、sans-serif）；不在线加载字体。常规 UI 14px，辅助文字不小于 12px，浮层标题 16px/600，正文行高 1.5；提词字号建议 28–40px、行高 1.6–1.8，可按用户设置调整。计时与尺寸用等宽数字。

间距使用 4、8、12、16、24、32px；按钮/输入常规高度 36px，主录制按钮 40px，图标按钮命中范围至少 32×32px。控件圆角 8px、Popover 12px、独立提词/相机窗 12–16px；不堆叠卡片或把所有按钮做成胶囊。只给独立浮窗轻阴影。窄屏优先收起次要控件，不隐藏停止按钮；窗口贴近边缘时浮层翻转并留 12px 安全间距。

## shadcn/ui 组件映射

| 功能 | 首选组件与要求 |
|---|---|
| 主次操作 | Button variants；图标附文字或可访问名称 |
| 输出尺寸 | Select + SelectGroup，按竖屏/横屏/其他/自定义分组，不按平台重复 |
| 自定义宽高 | FieldGroup、Field、Input，宽高各有标签；错误就近显示 |
| 摄像头/麦克风 | Select、Switch；失效设备保留可理解的提示 |
| 提词设置 | Popover、Textarea、Slider/Select、Tooltip |
| 状态与反馈 | Alert、Progress、Spinner、必要时 Sonner；保存结果持久显示 |
| 退出确认 | AlertDialog；说明保存后退出与取消的后果，按钮文字明确 |
| 分隔 | Separator 与留白，不建立无意义嵌套 Card |

Dropdown/Select 条目使用正确 Group 结构；Dialog 有 Title；表单错误使用 aria-invalid；Icon Button 有 aria-label；键盘焦点不被去掉。间距用 flex/grid gap；相同宽高用 size；条件样式用 cn。不会为了显示组件库能力而加入无业务需要的组件。

## 必须设计的状态

准备、权限缺失、设备缺失、倒计时可取消、真实录制中、提词暂停、保存中、全部保存、故障中断但已保存、部分保存、保存失败、摄像头关闭、自定义尺寸非法。不要用一张主界面稿冒充所有状态已经设计完。

- 倒计时至停止锁定区域、尺寸、显示器、设备、相机采集开关、音频模式和目录；提供锁定原因。
- 后台确认开始后才启动计时和提词，前端按钮不能伪造录制成功。
- 停止/保存期间避免重复提交；保存中不能提示已完成。
- 提词暂停只停文字，不停录像；稿尾不停止录像。
- 结果显示实际文件和实际成功状态。部分保存必须保留成功文件、指出失败项并等待用户确认。
- 不可用尺寸说明原因，不静默降级。麦克风故障不能静默变成无声录制。

## 可访问性与动效

正文文本对比目标至少 4.5:1，大号文字及关键非文本控件至少 3:1；须在实际渲染中验证透明度和背景组合。键盘顺序按视觉顺序，Escape 关闭当前设置层但不停止录制，输入框内不触发全局快捷键。工具条淡出时保留可发现的停止入口，hover/focus 恢复；尊重减少动态效果设置。反馈动画控制在 120–180ms，禁用持续装饰动效。

## AI 交付自检

- 先声明所用选稿版本；截图文字与用户已确认规则冲突时以规则为准。
- 保留 12 预设及 240–3840 偶数自定义，屏幕输出和相机原片不可混淆。
- 不生成假设备、假权限、假成功文件充当正式功能；纯设计稿/临时演示须明确标识。
- 验证准备/录制/保存/错误状态、键盘操作、不同窗口大小和浮层边缘，不只检查静态截图。
- 原生穿透、窗口排除和真实保存必须原生验收；浏览器视觉正常不等于这些能力成立。
- 未获正式开发批准，不安装组件、初始化工程或迁移正式 UI。用户选稿后只收敛设计规范，代码实施仍受独立批准约束。

## 来源与待定

官方主题规范：https://ui.shadcn.com/docs/theming
官方项目配置：https://ui.shadcn.com/docs/components-json
视觉稿索引：[三张候选稿](docs/design/2026-09-24/README.md)。
待用户选定：三张视觉方向中的一张、最终表面明暗及布局细节。选稿后补齐其余状态设计；不擅自把候选方案合成最终版。
