# RecordReady 前端设计标准

2026-09-25 · 供 AI 与开发者使用的设计交付稿，用户已审阅通过。用户已选定六张视觉稿，并明确跳过额外状态出图。设计审阅通过不代表正式开发授权。

后续授权记录：用户已另行明确回复“开始”，Mac 第一阶段正式开发现已获准。实现与验收进度见 [开发记录](docs/Mac第一阶段开发记录.md)；第二阶段和分发仍需结果评审。

## 依据与优先级

- 视觉依据：[六张设计稿](docs/design/2026-09-24/selected-direction/README.md)。沿用浮动线框、深底提词、摄像头预览、底部控制条和就近设置浮层。
- 逐图修正：[设计校正说明](docs/design/2026-09-24/selected-direction/校正说明.md)。原 PNG 保留；校正说明覆盖其中的生成文字、状态和锚点错误。
- 产品语义以 [CONTEXT.md](CONTEXT.md) 和 [MVP 方案](docs/口播录制工具-MVP方案.md) 为准。视觉稿不能新增功能或覆盖录制约束。
- 不复制截图里的设备名称、计数、摄像头人物、桌面壁纸为产品数据。虚化桌面仅用于展示，不给实际桌面或视频增加模糊效果。

## 技术与组件约束

正式开发批准后采用 React、TypeScript、shadcn/ui（Radix）、Tailwind CSS、Lucide React；语言使用 i18next + react-i18next。这是选定方案，不是已安装依赖的声明。

| 场景 | 组件与规则 |
|---|---|
| 普通动作 | Button；主动作实色，次动作 outline/ghost |
| 摄像头启用、镜像 | Switch + 可点击 Label；镜像只影响预览 |
| 设备、语言、字号、速度 | Select；禁止用装饰文本模拟可选项 |
| 录音模式、外观、预览位置 | RadioGroup；互斥值必须有选中语义 |
| 设置浮层 | Popover；锚定对应按钮，避开屏幕边缘，Esc 关闭后焦点返回入口 |
| 稿件编辑 | 独立可交互浮层，Input + Textarea；与正文穿透区分开 |
| 退出保护、需确认的失败结果 | AlertDialog；提供明确动作，禁止静默关闭 |
| 校验与反馈 | 行内错误 + Alert；Toast 仅用于非关键反馈 |
| 图标入口 | Tooltip + 本地化 accessible name；不靠图标形状猜功能 |

组件默认样式必须使用下列语义 token 调整，不用另一套组件库拼接。原生窗口的穿透、焦点、层级和采集排除不能用 DOM 样式冒充实现。

## 颜色与外观

外观选项为跟随系统 / 浅色 / 深色，默认跟随系统。橙色是品牌强调色，红色用于录制和错误。以下为规范值，非截图取色测量结果。

| 语义 token | 浅色 | 深色 |
|---|---|---|
| background / card / popover | #FFFFFF | #202226 |
| foreground / card-foreground / popover-foreground | #18181B | #FAFAFA |
| muted / secondary | #F4F4F5 | #2C2F34 |
| muted-foreground | #52525B | #B4B4BC |
| secondary-foreground | #27272A | #FAFAFA |
| border / input | #D4D4D8 | #707078 |
| primary | #C2410C | #FB923C |
| primary-foreground | #FFFFFF | #18181B |
| accent（选中浅底） | #FFF7ED | #432A1D |
| accent-foreground | #9A3412 | #FDBA74 |
| ring（焦点） | #C2410C | #FB923C |
| destructive / recording | #B91C1C | #F87171 |
| success | #15803D | #4ADE80 |
| warning | #92400E | #FBBF24 |
| capture-frame | #F97316 | #FB923C |
| prompter-background / foreground | #202226 / #FAFAFA | #202226 / #FAFAFA |

鲜橙用于线框和装饰；小号白字按钮用较深橙色以保持可读性。状态必须同时有文字或图标。准备就绪使用中性色标识，不用红点冒充录制中。浮层默认不透明，避免任意桌面背景破坏对比度。所有透明效果须单独检查实际背景下的可读性。

主题偏好持久化并跨浮窗同步；首帧应用，跟随系统时响应系统变化，手动选择时保持用户选择。换主题不改变桌面、摄像头原片和录制状态。

## 字体、尺寸与排版

- 系统字体栈：system-ui、-apple-system、BlinkMacSystemFont、Segoe UI、sans-serif；中文使用系统中文字体回退。
- 正文 14px/1.5，辅助说明 12px/1.5，浮层标题 18px/1.4、600，计时 20px 并用 tabular-nums。关键操作不小于 14px。
- 提词默认 32px/1.6，提供 24/28/32/36/40/48px；不随语言切换改变用户字号。速度选项慢/适中/快，具体滚动速度在实现时标定，不把倍速当口播准确率承诺。
- 间距使用 4、8、12、16、24、32px；普通浮层内边距 20px，表单组间 16px。控件高 40px，图标 20px、线宽 2px，点击区域至少 36×36px。
- 圆角：输入和按钮 8px，浮层和预览 16px，控制条 20px。边框 1px；浮层阴影建议 0 12px 36px rgba(0,0,0,.16)，深色降低阴影并依靠边框区分。
- 摄像头/声音/应用设置浮层约 320–360px，尺寸面板 400px，稿件编辑 480px；这是逻辑像素，不是录制输出像素。可用宽度不足时缩至屏幕留白内，正文滚动，主动作保持可达。
- 控制条内容自适应；英文允许加宽或两行布局，不缩小文字、不截断开始/停止等核心动作。浮窗距可用屏幕边缘至少 16px；考虑 Dock 与菜单栏。
- 提词工具条顺序固定：编辑稿件、字号、速度、回到开头、开始/暂停/继续提词。宽度不足允许工具条换行。

## 布局与操作

录制区域准备时可通过顶部把手移动、四角等比例缩放；倒计时到录制结束保持固定。改变桌面区域大小不改变输出分辨率。提词区独立移动；正文穿透，拖动把手、工具条与设置接收输入。编辑稿件不隐藏录制线框、不自动放大或移动提词区。

底部顺序：状态、计时、摄像头入口、声音入口与输入电平、保存位置、应用设置、开始/停止录制。摄像头缩略图齿轮与控制条摄像头入口均打开摄像头设置；应用设置只由控制条齿轮打开。设置浮层同时只打开一个，语言下拉属于其内部子层。

摄像头小窗/铺满、四角位置与手动拖动只影响预览；原片保持设备画幅。关闭摄像头后隐藏预览，保留控制条入口。设备名称来自真实枚举。声音模式明确为麦克风录音或静音录制；摄像头开启时同源麦克风音轨写入两份视频，关闭时仅写入屏幕视频。静音模式无音轨，工具条持续显示静音图标和文字。

准备时可以独立开始提词；初始按钮为“开始提词”。录制开始不因主题/语言变化重置提词。录制期间字号、速度、提词暂停/继续和位置仍可操作；稿件编辑锁定以避免正文突变。回到开头将提词暂停并复位，不停止录制。

稿件输入即时保存为本地草稿，“完成编辑”关闭编辑浮层；关闭按钮不丢弃已输入内容。字数由真实文本计算，排除换行，按 Unicode 字符计数；不照抄图片的 86 字或 7/50。图片中的 50 字上限不是确认的产品限制，不据此截断内容。

## 尺寸

| 分组 | 宽×高（像素） |
|---|---|
| 竖屏 9:16 | 720×1280、1080×1920（默认）、1440×2560、2160×3840 |
| 横屏 16:9 | 1280×720、1920×1080、2560×1440、3840×2160 |
| 其他 | 3:4 1080×1440、4:5 1080×1350、1:1 1080×1080、4:3 1440×1080 |

自定义宽高均为 240–3840 的偶数整数，明确点击“应用尺寸”后生效。非法输入不覆盖当前有效值；提交后在字段附近显示错误。合法 1080×1920 不显示报错。实际能力不足时标明原因、阻止开始，不静默降档。提供所有菜单项不等于已验证全部编码能力。

## 未单独出图的状态规则

用户已跳过额外状态稿，以下文字规则替代出图要求，不删除功能与验收要求。

| 状态 | 文案与动作 |
|---|---|
| 准备 | 准备就绪 / Ready，开始录制 / Start recording；未满足条件时显示具体原因 |
| 倒计时 | 3、2、1，取消 / Cancel；锁定区域、设备、尺寸、录音模式、目录；取消返回准备 |
| 启动中 | 正在启动 / Starting；真实采集启动后才进入计时，不提前表示录制成功 |
| 录制中 | 红点 + 录制中 / Recording + 计时；停止录制 / Stop recording；不提供录像暂停 |
| 保存中 | 正在保存 / Saving；禁用重复停止、再录一条；不伪造进度百分比 |
| 全部保存 | 已保存 / Saved；逐项列真实文件、尺寸、时长、目录，打开文件夹、再录一条；无摄像头时只列屏幕文件 |
| 中断 | 录制已中断 / Recording interrupted；同时列原因及各文件真实保存结果，不能仅显示“已保存” |
| 部分保存 | 部分文件已保存 / Some files saved；成功与失败逐项说明，保留成功文件，显示错误与残留位置，明确确认 |
| 全部失败 | 保存失败 / Save failed；可查看详情、打开残留目录（确有目录时），不能假称可恢复 |
| 权限/设备/目录/空间异常 | 开始前阻止录制并提供设置、重试或选择目录等对应动作；必需设备录制中故障结束整次会话并尽力保存，不自动静音 |
| 退出保护 | 录制中：继续录制 / Keep recording、停止并保存 / Stop and save；保存中等待，失败结果需确认后退出 |

录制中主题、语言切换即时生效且不重建采集、不重置计时。保存位置浮层显示当前目录和输出说明，选择目录调用系统选择器；不承诺系统弹窗可由应用重新设计。

## 国际化与可访问性

资源采用 zh-CN、en；首次中文系统用 zh-CN，其余用 en；显式选择持久化并跨窗同步，缺译文回退英文，不显示 key。界面、Tooltip、accessible name、校验和应用错误统一走翻译资源；不拼接句子。原生错误通过稳定错误码与参数映射，原始系统信息放详情。

用户稿件、设备名、路径和用户文件名不翻译。计时固定 mm:ss / hh:mm:ss，日期和普通数字用 locale 格式化，文件标识不随语言改名。语言菜单始终显示“简体中文 / English”。

| 中文 | 英文 |
|---|---|
| 编辑稿件 / 完成编辑 | Edit script / Done |
| 字号 / 速度 / 回到开头 | Font size / Speed / Back to start |
| 开始提词 / 暂停提词 / 继续提词 | Start prompter / Pause prompter / Resume prompter |
| 摄像头设置 / 声音设置 | Camera settings / Audio settings |
| 应用设置 / 外观 / 跟随系统 / 浅色 / 深色 | App settings / Appearance / System / Light / Dark |
| 录制区域与尺寸 / 应用尺寸 | Capture region and size / Apply size |
| 静音录制 / 保存位置 | Record without audio / Save location |

键盘 Tab 遍历可交互控件；焦点环清晰，Esc 仅关闭最上层设置，不能当停止录制快捷键。危险动作不能只靠颜色。错误关联到输入字段；状态提示使用合适的 live region，计时和电平不逐帧播报。尊重减少动态效果偏好。截图无法验证真实键盘行为、原生穿透、读屏和对比度，须在正式实现后实测。

## AI 交付检查

按六张图检查视觉结构，按校正说明检查细节；不重新设计为侧栏工作台，不新增登录、云端、视频库或剪辑页。先复用组件、再调 token；记录规范无法满足之处，不自行删减要求。

正式开发批准后检查中文浅色、中文深色、英文浅色、英文深色和跟随系统切换；验证长设备名、路径、英文按钮、小屏浮层及键盘焦点。主题/语言切换前后录制文件和计时连续。通过真实采集确认所有辅助窗口被排除。当前文档没有宣称上述行为已实现或验证。
