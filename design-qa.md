# UI 对齐检查 — 2026-09-25

final result: blocked

## 当前结论

已按六张定稿及 design.md 对齐控制条、摄像头/声音/尺寸/编辑/外观设置的视觉。局部窗口经过实际应用截图检查和第二轮修正；完整原生浮窗组合仍未验收。新签名包点击准备返回 screen_permission，不能用局部面板通过替代全场景通过。

依据：`docs/design/2026-09-24/selected-direction/` 六张 PNG 与校正说明。最新截图见 [截图目录](docs/design/2026-09-25/ui-alignment/README.md)。参考为约 1487×1058 桌面构图，实际为原生窗口逻辑像素截图，对照内容区域和 design.md 尺寸，不做混合尺度像素评分。相机/声音/尺寸/编辑与英文深色的参考和更新截图已放入同次视觉输入对照。

## 发现与修正

- [P2 已修正] 根字号 14px 令 rem 控件/文字整体缩小；现根字号16px、正文14px，恢复开关/输入/按钮可读尺寸。
- [P2 已修正] 主控制条大面积空白与矩形宿主：内容紧凑排列、圆角和透明宿主，中文/英文核心按钮完整显示。
- [P2 已修正] 摄像头纵向表单和默认单选圆点：横排标签/控件，分段选择保留 RadioGroup 语义；声音录音模式保持可见单选按钮。
- [P2 已修正] 原生灰色连续电平：改为分段显示，仍读取原始输入电平，未新增“声音正常”判断。
- [P2 已修正] 尺寸图标、其他比例标签与选中勾缺失；选中态恢复浅底描边。
- [P2 已修正] 第一轮尺寸主按钮被裁切：调整面板高度和内部间距，最终截图按钮及下方说明完整显示。
- [P2 已修正] 编辑器字数被 Field 默认宽度推到左边：设独立宽度并置输入框右下，补标题图标及48px完成按钮。
- [P2 已修正] 浮层入口提示缺失：使用 Radix Arrow，按实际锚点方向与位置显示；保留原来的设置打开/关闭行为。
- [P2 已修正] 英文提词工具条换行可能被短窗口裁切：前端实测第二行底部85px，原生英文宿主设96px；中文64px。实际原生组合仍待授权。
- [待验收] 提词正文、工具条、相机和录制线框的整体比例、位置、层级，以及新透明宿主的实际桌面边缘点击行为与采集排除。

## 行为边界

没有修改设备选择、录制/保存/退出保护、提词滚动或配置持久化逻辑。比例/尺寸入口仍打开现有尺寸设置，不新增独立快捷切换行为；没有增加音频正常/异常判定。后续功能变化需用户确认。

透明窗口按 Tauri 已安装配置规范启用 macOS private API，仅用于当前已批准的 Mac 内部/独立应用方向；不适用于 Mac App Store。该发布渠道仍在既定延后范围内，未开展分发。

## 验证

前端4项测试、lint、TypeScript/Vite、原生测试、Tauri打包及严格签名验证通过；lint仅原有两个组件热更新警告。实机检查设置页的中英文/浅深色、Esc返回，未改变稿件和录制尺寸；恢复中文浅色。

固定包 cdhash `6f22ad8148fc61ed0081e47f8f83539118fff6e7`，可执行 SHA-256 `7486e97c4bcc805d645880b2776839564a71276ca7802d0b7c30af256bbc28b0`。后续只授权与复核，不重建本包。
