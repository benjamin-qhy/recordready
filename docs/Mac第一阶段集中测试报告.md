# Mac 第一阶段集中测试报告

日期：2026-09-25。测试使用固定完整临时签名包，cdhash `aeb6a3bb04183817b8eaf46601008031984e450f`。本轮没有重新打包或重签。原始录像仅保留本机。

## 已验证

| 项目 | 证据与范围 |
|---|---|
| 同版本授权复用 | 多次退出、重启和设备准备，不再要求屏幕授权 |
| 横竖 1080 屏幕固定 30fps | 实录逐帧检查，异常间隔为 0，双视频完整解码 |
| 自定义/静音/关闭相机 | 1000×800 实录仅一个视频轨，无相机文件；1001×800 拒绝且不覆盖配置 |
| 连续会话和倒计时取消 | 多个独立目录；取消回准备状态 |
| 录制配置锁定 | 录制时预设与自定义尺寸控件禁用；原生生命周期测试通过 |
| 拍手音画粗测 | 两次约 23ms 偏差，约 33ms 帧级测量精度；不能代替屏幕/相机物理同步标定 |
| 设置窗关闭再打开 | 系统关闭按钮隐藏窗口后可重开 |
| 提词命令 | 开始变为暂停，暂停/复位回开始；仅证明状态，不证明实际阅读位置 |
| 稿件持久化 | 78 字临时稿关闭窗口、重启后仍保留；测试后恢复原空稿 |
| 语言/主题 | 显式英文、中文切换成功；英文和跟随系统选择重启保留，随后恢复中文浅色。系统外观变化时的实时跟随未测 |
| 异常保存 | 无设备原生测试注入错误与目标同名文件：成功文件保留、失败 partial 文件保留、同名文件不覆盖、结果为 partial；全失败不误报 saved |

## 不通过

1. **高优先级：Command-Q 绕过录制退出保护。** 录制中约 20 秒执行快捷键后进程直接退出。`Session-2026-09-25T07-14-38Z-838FF2` 仅留下两个 `.partial.mp4`；屏幕文件缺少 moov，不能正常播放。Tauri `ExitRequested` 保护没有覆盖这条实际退出路径。该测试录像是专门创建的测试数据，未影响已保存会话。
2. **主题/稿件变化可能使另一窗口意外切到英文。** 未保存语言偏好时 storage 监听错误默认英文。源码已修复，不在本轮固定包中，待下轮回归。
3. **录制配置不跨重启保留。** 自定义尺寸、相机开关、音频模式重启恢复默认值；与外观/语言/稿件持久化行为不同，需依据设计补齐。

## 仍需实机专项，不能标记通过

- 原生线框与提词把手拖动、四角等比例缩放、正文鼠标穿透：当前工具观察绑定的是 Tauri 窗口，未取得这些独立原生面板的可操作定位，不盲点桌面坐标。
- 提词字号、速度、滚动到底、暂停位置和复位的实际画面效果；提词设置重启后的原生效果。
- 逐个辅助窗口在录制区域内的排除覆盖。已有成片抽帧未见设置窗/提词，但不足以覆盖所有窗口和位置。
- 实际拔出/断开摄像头和麦克风。只有错误注入测试，不能替代硬件实测。
- 磁盘写满、写入中权限变化、保存过程中退出。没有通过填满用户磁盘制造故障；目标文件冲突仅覆盖一种保存失败。
- 其余高分辨率、显示器切换和负载表现；部分设备/显示器选择功能尚未实现，无法验收。
- 原生 Command-Q 修复和前端语言修复需要下一测试构建验证，按约定先完成所有修改再统一签名与授权。

30 分钟长录按用户决定排除。第一阶段尚未验收通过，优先修复退出造成录制损失的问题。

## 集中修复与第二轮回归

统一修复包 cdhash：`0e9edc6cf4e834de818da63e6257f882db1e5221`；可执行 SHA-256：`94bdd1984f39a1ab0be1c5f3bce2cdd03840005c580f9b8ca46a6ea6ad88d125`。完整签名严格校验通过。

- 原生退出：AppKit 菜单原本直接发送 `terminate:`，绕过 Tauri 事件循环。增加保留并转发原 delegate 消息的退出代理，原生退出先显示确认；明确确认且会话不忙时才退出。实际 Command-Q 已进入退出保护，确认退出成功。**录制中回归待重新授权后执行。**
- 录制配置：尺寸、摄像头、麦克风及保存目录写入原生偏好；重建 Recorder 自动恢复。原生隔离偏好测试通过，实际 1280×720 配置退出再启动保留。
- 提词参数：字号、速度使用原生偏好作为唯一来源，UI 从实际状态读取，避免只保存网页值却未恢复原生效果。40px/慢速重建恢复的原生测试通过。
- 主题触发语言变化：无保存语言时不再强行切换英文；已包含在统一修复包中。
- 新增原生退出允许/阻止、配置/提词参数持久化回归，沿用保存失败、CFR、无音轨测试，全部通过；前端 4 项测试、TypeScript/Vite 和 debug 打包通过。

新包实录权限检查仍返回未授权，已按约定退出应用并请求用户重新添加当前包。尚未完成的物理交互/硬件测试不因此变为通过。当前等待授权，不再重建本轮包。

### 授权后实录回归

用户完成授权后，同一修复包实录 `Session-2026-09-25T07-36-57Z-FD8AF0`：录制中 Command-Q 显示退出保护，直接退出按钮禁用；“继续录制”返回且计时不中断。再次 Command-Q 后“停止并保存”产生两份正常 MP4。保存后退出按钮可用，确认后进程退出。此前会造成未封装文件的 Command-Q 路径已通过本轮实测。

本段约 43.76 秒，屏幕 1280×720、1312 帧、30/1 fps，异常帧间隔 0；屏幕与 1920×1080 相机文件完整解码、时间戳无倒退。未在测试中重新打包/签名。其余专项未测项仍保留，未宣称全部第一阶段验收完成。

后续交互：当前包中字号切到 40px、速度切到慢，实际下拉值正确，编辑 411 字测试稿后控制条仍保持中文。已准备原生浮窗供真人检查：录制框移动/等比例缩放、提词独立移动、正文点击穿透。工具未取得这些面板的可定位区域，因此已请求用户逐项报告，不把请求本身当作测试通过。测试稿此前为空，完成后应清空恢复；字号/速度原值为 32px/适中，完成后恢复。

用户回复“都可以”，确认录制框把手移动与拖角等比例缩放、提词区独立移动、正文点击穿透三项实机通过。该证据为用户目视/操作确认，不是工具自动测量。随后已启动长稿慢速滚动，等待用户报告实际滚动效果；尚未确认暂停位置与复位画面。

用户进一步回复“滚动正常”，确认慢速长稿滚动通过。已通过键盘定位并触发暂停，按钮变为“开始提词”，等待用户确认实际停留位置；复位尚未验证。

用户回复“暂停正常”，确认暂停后阅读位置保持且停止移动。随后操作“回到开头”，界面保持未播放，等待用户确认正文返回第一段。

用户回复“复位正常”，确认正文返回第一段并保持暂停。至此，本轮提词慢速滚动、暂停位置保持、复位及上述拖动/穿透均由用户实机确认通过，无新增异常。已通过应用界面清空临时 411 字测试稿、恢复 32px/适中速度，点击完成编辑同步到原生提词正文，随后正常退出释放设备。完整第一阶段验收仍需按报告中的其他专项和未实现功能分别处理。

## 功能补齐后的固定包（2026-09-25）

当前用于下一轮集中验收的包：`desktop/src-tauri/target/debug/bundle/macos/RecordReady.app`。cdhash `bc8a3ad78af7186554796c869d23a54cfb9b50b0`，可执行 SHA-256 `9c9df602dc6ec88aef681fb662e464bfe9554fdabb17bbecd41d5500a44f237f`。完整签名与严格验证通过。后续授权期间不再重建或重签。

本次补齐真实摄像头/麦克风/显示器选择、预览镜像/布局/四角/归一化手动位置、就近设置浮层、提词工具条和区域尺寸入口。详见开发记录末尾。

- 实际设置窗口已验证：相机下拉含 MacBook Pro相机；音频下拉含 MacBook Pro麦克风及 OrayVirtualAudioDevice。已选择内建相机/麦克风并确认重启保留，显示器当前为 Built-in Retina Display。未连接第二个实体显示器，切换显示器实录未验收。
- Esc 首先关闭设备下拉、再次关闭浮层，焦点回到对应控制条入口；修正前焦点落到 webview body 的问题已在正式窗口回归通过。
- 中文浅色、英文深色设置与稿件编辑浮层实际渲染已检查；恢复中文浅色，稿件保持空。字号 32px、适中速度及 1280×720 输出未改。
- 前端 4 项测试、TypeScript/Vite、原生无设备测试及打包成功；新原生测试包含预览不影响录制状态/输出、设备不存在时配置原子性、预览偏好恢复和极窄区域/负坐标区域的几何边界。lint 只有已有两个 shadcn 组件 Fast Refresh 警告。
- 固定包点击“授权并预览”仍返回 `screen_permission`。系统设置中旧 RecordReady 项虽为开启，但新包尚不能使用该权限。应用已正常退出并确认无运行进程，系统设置停留在“隐私与安全性 → 录屏与系统录音”。需要用户移除旧项、添加本次固定包并开启；不由工具代操作授权。

本次没有新录制文件；此前基础录制、退出保护和提词交互的通过结论保留，不要求重复整套基础测试。待授权后集中检查新增镜像/铺满/预览拖动、工具条定位及全部辅助窗口排除，再补高分辨率、设备断开保存、保存中退出。真实磁盘写满仍未测。没有提交、push 或创建 PR，未启动第二阶段。

### 固定包授权后的新增功能集中实录

用户重新授权后，以上同一固定包正常准备设备并显示麦克风电平；本轮未重建或重签。初次准备耗时较长，最终进入准备就绪，未持续报权限错误。

| 会话 | 实录与检查结果 |
|---|---|
| `Session-2026-09-25T08-22-14Z-5A82A2` | 1280×720，约 42.39 秒，屏幕 1271 帧；30fps 异常间隔 0。屏幕和相机完整解码，无倒退时间戳；两份视频解码后的 PCM 音频哈希一致。 |
| `Session-2026-09-25T08-26-22Z-C5B1A1` | 3840×2160，约 44 秒，屏幕 1319 帧；30fps 异常间隔 0。两份视频完整解码，无倒退时间戳；相机最大帧间隔约 66.7ms，不能据此宣称相机无掉帧。 |
| `Session-2026-09-25T08-30-53Z-BACB4B` | 2160×3840，约 21.80 秒，屏幕 653 帧；30fps 异常间隔 0。两份视频完整解码，无倒退时间戳；相机最大帧间隔约 35ms。 |

录制中相机启用/设备选择禁用，预览设置仍可修改。横屏 4K 录制过程中从铺满切换到小窗、左上、关闭镜像，计时继续且最终保存成功。各段相机文件均为 1920×1080，预览布局未改变其输出尺寸。此为状态和文件证据，镜像方向及原片像素未作人工对照标定；固定屏幕 30fps 也不等于每帧均是不同的采集图像。

720p 第 20 秒及横屏 4K 第 25 秒屏幕抽帧未见实际相机预览、提词正文或设置浮窗。录制背景为 Codex，聊天中的工具截图缩略图属于被录制应用内容，不算 RecordReady 浮窗直接泄漏。录制框边缘、四角、区域工具条及不同位置尚无逐一覆盖证据，不能标记全部辅助窗口排除通过。原始录像与相机抽帧仅留本机 Movies 目录，不进入仓库。

停止录制后立即尝试 Command-Q 时，保存结果已经出现，未捕获保存进行中的状态；因此保存中退出仍未实测通过。没有可供拔出的实体外接摄像头/麦克风，也未进行实体断开测试。真实磁盘写满、多显示器切换和物理音画同步仍未测。已请求用户确认铺满预览及提词工具条完整显示，尚未收到该项确认；不将授权回复视为视觉验收。

测试结束通过应用界面清空临时 52 字稿件并完成编辑，恢复 1280×720、右下小窗、镜像关闭；字号 32px、适中速度、中文浅色保持原值。应用正常退出并检查进程不存在。第一阶段功能补齐与高分辨率实录已有证据，但完整视觉/硬件专项仍待验收；尚未提交、push 或创建 PR，未进入第二阶段。

### 交付前检查（2026-09-25）

用户明确要求按界面验收、专项测试、审查及 Git 交付三步执行；确认没有可拔出的外接摄像头/麦克风，设备断开记录为未测。本轮沿用已授权包，可执行 SHA-256 再次核对一致；没有重签或重打包。

重新执行前端 4 项测试、lint、TypeScript/Vite 和原生测试，均通过；lint 只有两个组件热更新警告。原生测试实际验证合成 MP4 编码与固定帧率、注入故障后的部分保存、同名文件保留及全失败，未将模拟故障标记成硬件断开通过。

代码审查覆盖采集过滤器、设备选择、写入/封装、会话锁定、原生退出代理、Rust 桥接与窗口定位。屏幕过滤器按当前应用进程排除所有窗口，不依赖逐个窗口 ID；但完整窗口位置覆盖仍欠实录证据。发现的交付限制：部分底层错误原文直接展示，尚未全部采用稳定错误码与本地化详情；多显示器坐标仅有几何测试与实现检查，无实体多屏证据。

本次以内部候选版本交付，PR 保留待验收项，不表示第一阶段全部通过，不自动合并或进入第二阶段。

#### 真实空间不足补测（隔离卷）

新增 `bash desktop/scripts/test-disk-full.sh`：建立 64MB 临时 HFS+ 磁盘映像，先向真实 AVAssetWriter 写入合成视频，再通过实际字节写满该卷并确认 POSIX `ENOSPC`，随后继续送帧。结果：append 报错，finish 不返回 saved，预先保存的哨兵文件内容保持完整。临时卷已卸载并清理；没有填充系统磁盘，没有改动固定测试应用包。

这是实际文件系统空间耗尽下的原生写入测试，不是手工注入错误码；但不覆盖主磁盘写满时的整个应用 UI、双路实机采集或系统整体低空间行为。保存进行中退出仍只有状态锁定/原生退出保护的代码与既有测试证据，没有捕获到真实保存中的 UI 操作。

本轮 `cargo check --locked` 亦通过。

## UI 对齐后的待验收固定包

cdhash `6f22ad8148fc61ed0081e47f8f83539118fff6e7`，SHA-256 `7486e97c4bcc805d645880b2776839564a71276ca7802d0b7c30af256bbc28b0`。UI局部已截图复核，自动测试/构建/签名通过。该版本准备设备实际返回 screen_permission，尚无新实录。需要用户移除旧屏幕录制授权项、添加当前 bundle 中的 RecordReady.app 并开启。原包实录结论不自动扩展至新透明宿主；本轮需补完整浮窗视觉与采集排除。应用已退出，稿件及尺寸保持原值，语言/主题恢复中文浅色。

## Screen Studio 操作对齐：正式项目改造（2026-09-25）

本轮按用户最新五项要求修改真实桌面代码，不改动原型。统一测试包 cdhash `374d0b1da2976c7c90063ddb1711f8d973d9e94d`，SHA-256 `1791e7e862c90ac7ef24b0e4a3650be3396826b38df6d54f0bfe3b11fa6f4775`。包位于 `desktop/src-tauri/target/debug/bundle/macos/RecordReady.app`。

已通过：

- 前端 Vitest 5 项；lint 无错误，保留两个组件热更新警告；TypeScript/Vite、Rust/Tauri debug bundle、签名校验通过。
- 原生测试覆盖区域坐标校验、整体移动、锁定穿透、选区外遮罩及无摄像头授权时仍可选择区域；原有写入和保存故障测试通过。
- 实际应用导航顺序、真实设备菜单、关闭摄像头、静音、恢复麦克风、摄像头关闭时禁用预览子菜单均核对。
- 实际区域面板显示当前范围、坐标、显示器、输出比例和开始按钮。拖动面板空白处 50×20pt 后 X/Y 从 573/151 变为 623/171；尺寸未改变。尺寸设置打开和 Escape 返回焦点正常。
- 中文浅色与英文深色实际渲染；设置、稿件、保存目录入口可打开。错误内容较长时设置浮层可滚动。左侧退出按钮在空闲状态退出，经进程检查确认。
- 静态审查修复首次录制前摄像头预览缺失、控制条增高后停止按钮可能落到屏幕底部之外两项问题；复审未发现阻断性代码问题。

受阻与未验证：

- 区域面板直接开始实际调用录制准备，但返回 `screen_permission`。已请用户在系统设置重新添加并启用当前固定包；无新版实录，不将旧包高分辨率录制结论当作本轮通过。
- 恢复摄像头选择时停留“正在准备”。tccd 日志确认旧摄像头授权 cdhash `bc8a3ad78af7186554796c869d23a54cfb9b50b0` 与当前包不匹配，并在 22:48:17 发起 Camera `AUTHREQ_PROMPTING`；等待用户处理系统提示。预录制摄像头取景仍未现场验收。
- 新版完整桌面叠层、选区内部拖动/四角缩放、录制时穿透、所有辅助窗口排除、保存中退出待授权后集中实录检查。无实体外接设备，断开测试仍按用户要求记为未测。
- 轻微拖动尾帧丢失可能性（IPC 忙时释放指针）仅为代码审查提示；当前面板50×20pt移动实测正确，未观察到该问题。

截图位于 `docs/design/2026-09-25/studio-alignment/`，仅截取无私人摄像头画面的应用窗口，不代表全桌面合成截图。中文浅色已恢复，麦克风恢复原内置设备，摄像头选择已恢复但系统授权待处理。此次没有合并或进入第二阶段。

## 2026-09-26 尺寸下拉与口播稿缩放

测试包 cdhash `d7e69f593ce99c2428a88e062b37e2c052e3a880`，SHA-256 `4e3abdb847e3aa29c65cffabb71e8a976e21d835d832558927088d1ab6b013b8`，取代上一固定包。

- 前端5项测试、lint、TS/Vite、Tauri构建和签名校验通过（两个既有热更新警告）。
- 原生测试通过：提词区八方向几何缩放、极端边界、最小尺寸、播放中缩放保持状态、滚动偏移80保留、隐藏清理以及响应式工具条高度。
- 实际桌面确认：稿件只有正文；编辑窗口400×450通过右下角放大为523×523，再通过左边缩窄为463×523，正文随尺寸铺满。
- 实际区域输出使用下拉列出12个预设和自定义。选中1920×1080后输出及选区比例立即改变，没有独立尺寸窗口。测试结束当前输出1920×1080（恢复操作遇到用户接管，保留现场）。
- 静态审查发现并修复工具条在屏幕边缘与正文错位问题：Rust不再给原生提词工具条重复加16pt内边距。
- 播放提词区的八方向缩放有原生状态/几何测试，尚未完成真人桌面拖动与录制中实际操作验收；本轮没有重新做真实录制，上一轮权限限制不视为解除。

## 2026-09-26 口播稿入口与原位编辑

新包 cdhash `a44db358a801111ff9f801eed3d53779787fac05`，SHA-256 `7af65ae2fe3ff2227924fede15133810582b84b086c191499461c868698cd43a`。

- 区域与口播稿显示标志分离。口播稿使用原生 NSTextView 直接编辑，正文由原生持久化管理；开始录制不再从旧网页 localStorage 回写覆盖稿件。
- 前端5项测试、lint（两个既有警告）、TS/Vite、Tauri打包签名通过。原生测试覆盖：Area不打开口播稿、口播稿独立打开不打开Area、正文保存、播放/暂停只读穿透、重置恢复编辑、录制期间编辑锁定、缩放与焦点恢复。静态审查无阻断项。
- 实际桌面验证口播稿打开正文与工具条，不再出现独立编辑浮层；点击“重置并编辑”后正文获得键盘焦点。粘贴两行临时中文后正文正确显示，随后清空恢复原空稿件。首次打开的自动焦点仍可能落在工具条，正文可点击编辑，重置操作已验证恢复正文焦点。
- 本轮未执行真实录制；不将原生状态测试等同于录制中实机验收。旧权限状态不视为新包已获授权。

## 2026-09-26 提词工具与面板精简验收

最终包 cdhash `c8d9a82bbb44a83625c7894f3fa21180c13d3754`，SHA-256 `5e92de8a03f0ff5ec9f9b33430fabb123868bf05b9a2f7a26056e56c67d2799d`。

实现 A−/A+ 字号、图标减速/加速与 pt/s 数值，Reset仅暂停并回到开头；正文单击编辑、双击选词、按住拖动，删除顶部三点把手。正文改为交互区（最新用户要求覆盖旧穿透规则），编辑会暂停播放，录制状态不禁止正文编辑。原生正文及网页工具条同步主题。区域面板只显示比例、自定义宽高和录制；显示器选项移动到设置。保存位置动态适配内容高度，导航区域/口播稿可见状态持续高亮，左侧关闭总是确认。

验证结果：

- 前端5项测试、lint、TS/Vite、原生测试、Tauri打包和签名校验通过；lint保留两个组件热更新警告。
- 实际应用确认保存位置完整展示且没有滚动条；区域下拉仅有9:16、16:9、3:4、4:5、1:1、4:3与自定义，自定义显示当前1920×1080；区域与口播稿导航分别可呈现on状态及持续高亮。
- 实际提词播放时字号28→30、速度24→26pt/s，状态仍为“暂停提词”（正在播放）；随后恢复28和24，重置返回“开始提词”，没有跳到编辑窗口。
- 单击正文聚焦、双击选词正常。首次拖动发现误选文字，修复为按事件坐标计算位移而非读取当前全局指针；重打包后拖动未再出现文字选择。原生几何测试覆盖移动与边界；未单独记录桌面绝对坐标差值。
- 实际浅色正文黑字白底、深色正文浅字深底；已恢复原深色偏好。用户已有稿件没有修改，未保存含稿件内容的截图。
- 空闲状态左侧关闭弹出“确定退出 RecordReady 吗？”，取消保留应用；退出确认与旧录制/保存保护共存。录制中的交互有原生状态测试，本轮没有新的真实录屏成片验收。

## 2026-09-26 Product Design 控件统一验收

最终包 cdhash `e29b43a0a5146525763a4ace40417c1eb84b2228`，SHA-256 `3b68abe0528b622466d9a9c012edb5e71c127bc9286517e6009fe3f95c0d2903`。

查明提词工具条一直深色的直接原因：`:root[data-view=prompter]` 强制覆盖全局主题。已移除，原生初始化也不再硬编码深色。主窗口负责发送解析后的主题，打开口播稿前显式同步。

- 前端5项测试、lint（2个既有热更新警告）、TS/Vite、Tauri打包和签名通过。原生测试覆盖背景alpha的有效边界、NaN/无穷拒绝、持久化、主题颜色、文字及窗口alpha保持1、全透明仍有命中背景。
- 实际UI确认浅色正文及工具条；字号/速度均为相同−/+与下拉。字号28→32、速度30→40可切换；最终恢复字号28、速度30。背景滑块实际调整到45%，最终恢复100%。普通窗口截图会被工具合成到不透明底色，透明背景叠加任意桌面的对比效果未从截图量化；未声称全场景对比度合规。
- 区域比例收窄，默认显示1920和1080，无应用按钮。无效宽度显示错误并禁用开始；输入1280后无需确认即应用、开始按钮恢复；最终恢复1920×1080。
- 保存位置与设置均240pt宽，内容全部可见且没有滚动条。前后截图在同一检查输入中对比，标题/按钮/边框/间距一致。
- 审查发现400ms自动应用前直接开始可能使用旧尺寸，已禁用未应用/无效尺寸时的开始按钮。
- 未改动用户稿件，未把正文截图保存到仓库。本轮没有执行新的真实录制验收。

截图和逐项设计检查记录见 `docs/design/2026-09-26/compact-refinement/README.md`。

## 2026-09-26 即时设备反馈、缩放与配置恢复（验收未完成）

最终包 cdhash `cebd2a36701ed771e0c93a763ec1cd123d523652`，SHA-256 `2dae28b768199dc778006043b652fbecfb144ca683c1907c4f4b19e5e93c39e3`。路径仍为 `desktop/src-tauri/target/debug/bundle/macos/RecordReady.app`。

自动检查通过：

- 前端5项测试、TypeScript/Vite、lint（2个既有组件热更新警告）、Tauri构建及严格签名校验、`git diff --check`。
- Rust新增2项测试：单个损坏配置不丢弃其他窗口位置、离屏/过大窗口工作区约束。
- 原生完整测试通过；本轮覆盖设备初始关闭/历史配置恢复、已授权设备监听规划、尺寸配置不重启设备、背景合成层非不透明、角等比/边自由几何、持久化与恢复、聚焦后8个子缩放窗口仍位于正文上方。原有录制/故障保存测试继续通过。

实机检查使用本轮中间包 `b7b1134d04fd413aadda2d0106a84905655f82ba`：

- 已有相机/麦克风选择保留，未被新的默认关闭覆盖；启动没有自动录制/播放。主导航有真实数据驱动的麦克风电平位，未就绪保持静止。
- Area与口播稿独立开启，导航均显示on。提词工具条可见字号/速度/背景/重置/播放/关闭分隔线。
- 提词正文从640×200角拉伸到704×220，比例3.2保持；随后右边拉伸到752×220，高度不变。正文未修改，未保存含稿件内容的截图。
- 发现正文聚焦会遮住缩放手柄内部半边；最终包已改为正文上方的子窗口并通过原生测试，最终包尚待桌面复测。
- 发现录制面板宽高标签换行；最终包改为简短宽/高（英文W/H）、保留完整可访问标签。640pt提词工具条恢复40pt宿主，去掉无关设备错误对提词面板的占位。

受阻/待测：

- 中间包实际提示摄像头权限未放行，未确认真实摄像头画面及声音电平；未执行新版完整录制。不能把原生模拟检查当作摄像头预览/麦克风实采通过。
- 实机操作途中Mac锁屏，工具无法解锁，已请用户手动解锁。最终包已生成，但锁屏时仍运行中间包；需解锁后正常退出旧进程再启动最终包。
- 最终包的手柄完整命中、主/设置拖动和跨启动位置恢复、透明背景与桌面合成、深浅主题/英文布局、相机齿轮锚点及实际录制待续验收。
- 测试现场提词尺寸752×220（原640×200），字号28、速度30、背景45%；正文未改。后续应先验证持久化，再通过UI恢复原尺寸。主工具栏拖动尝试未观察到保存坐标变化，需继续确认真实拖动命中和移动事件，不判通过。
- 如果已启用且获授权的设备缺失，组合监听明确报错，不自动换设备；健康设备单独继续监听尚未实现。物理拔插仍因无外接设备按用户要求记为未测。

## 2026-09-26 摄像头菜单展开与字号首次点击修复

最终包 cdhash `b1497cca5e548e65dd8f36127e058ddef954177a`，SHA-256 `7ee5959f0a90351467b84b7816265b3a558522fb705be4fc274222e4f3db3794`。

- 摄像头菜单取消“预览设置”子菜单，按布局（小窗/填满）、位置、镜像、录制设备排列并使用三处分隔线。齿轮和导航复用同一菜单构造；未开启相机时预览操作禁用。
- 复现旧版：正文获得焦点后，坐标点击工具条字号加号，焦点转移但26未变化；随后在已激活工具条点击可变28。Tauri的acceptFirstMouse默认false，导致第一次点击仅激活窗口。
- 修复：提词工具条宿主开启acceptFirstMouse；禁用按钮仍接收命中但不执行操作，避免事件落到可拖动背景。控件组默认普通光标，空白仍可拖动。
- 新包复测相同路径：正文焦点下直接坐标点击字号加号，首次即24→26，无需先点击速度；随后恢复字号24。速度20、背景50%、用户稿件及窗口几何未主动改动。
- 菜单实际AX确认预览选项全部为顶层、在“不录制摄像头”之前；原生测试确认三个分隔组、无子菜单、开启/关闭时可用状态。当前相机关闭，未为菜单测试强行开启设备，因此齿轮入口本轮未单独现场点击。
- 原生测试、前端5项测试、TS/Vite、lint（两个既有热更新警告）、Tauri打包签名、diff检查通过。重建后应用提示麦克风权限待放行，本轮未执行真实录制。

## 2026-09-26 摄像头设备/预览菜单分离与形状

最终包 cdhash `42691b396616ac3ab545f7112288020d46dcbcc8`，SHA-256 `e4b5551138f5d35690ac348873064ea1d36ed9187b7a6a35477397c1c6340be8`。

- 实机主导航菜单只显示“不录制摄像头”和“MacBook Pro相机”，已关闭菜单并保留设备配置。
- 原生测试验证：设备入口无预览操作；齿轮在无区域时只有“左右翻转”；有区域/小窗时包含布局、位置、方形/圆形、左右翻转；填满时无形状组；齿轮菜单无设备选项。
- 形状及渲染测试验证：选择圆形得到1:1窗口，偏好恢复保持圆形；视频遮罩裁切四角而包含中心；真实预览层对象使用形状遮罩，父层不裁切齿轮；八个缩放控件存在，单边拉伸可改变宽高比，遮罩同步到新尺寸。使用空AV session，不等同真实摄像头画面验收。
- 原生完整测试、前端5项测试、TS/Vite、Tauri构建签名和diff检查通过。
- 本轮新包仍显示摄像头权限提示；真实画面、齿轮现场展开和圆形裁切视觉效果待系统授权后实测，未执行真实录制。用户稿件、字号24、速度20、背景100%保持原现场值。

## 2026-09-26 录制控制条、暂停与填满联动

测试包 cdhash `1bce831f1ef3b80b57248b2b9df8eb09b19f7248`，SHA-256 `ce7d39e71cfbcb8f104d6095234fd02e3de8df73e762a1c7b12c3f77ee9e243b`。

- 原生测试先以缺失 `RecordingPauseClock`、未知 `pause` action 失败，再实现暂停时钟和状态动作。最终验证2–7秒暂停从9秒墙钟时间中扣除，输出时间为4秒；二次暂停后计时冻结。暂停/继续会同步切换Engine与Recorder状态。
- 采样路径在暂停时停止屏幕、摄像头和麦克风写入；输入电平仍可更新。恢复后所有采样统一扣除累计暂停时间，并丢弃采集于暂停期但延迟送达的缓冲，防止时间戳倒退；停止时尾帧刷新和元数据时长也使用扣除后的时间。
- 前端测试验证录制和暂停均只显示录制控制条、暂停仍锁定配置；Rust测试验证420×64控制条对600×400区域选择区域外位置。暂停纳入退出保护，暂停时停止可正常保存。
- 填满预览移动分支测试验证只在 `fill + Area可见` 时联动区域；实现中摄像头拖动直接调用区域移动，区域更新继续驱动填满预览。填满隐藏摄像头重复缩放手柄，小窗保留8方向缩放。
- 实机确认新版区域面板可打开、1080×1920和开始按钮正常。点击开始后当前测试签名仍返回 `screen_permission`，因此没有进入实际录制状态；主导航切换、暂停/继续按钮、录制中口播稿及控制条区域外位置尚未完成真实录制现场验收。
- 原生完整测试、前端5项测试、Rust3项测试、TypeScript/Vite、lint（两个既有热更新警告）、Tauri构建及严格签名、diff检查通过。没有修改用户稿件或主动改变设备选择。
